
  # project-safe

  This is a code bundle for project-safe. The original project is available at https://www.figma.com/design/7HzZBQCMHftzepWPOV6cwL/project-safe.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  